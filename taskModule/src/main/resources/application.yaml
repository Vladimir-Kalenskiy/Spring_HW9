server:
  port: 8081

spring:
  application:
    name: TaskModule
    generate-ddl: false
    hibernate:
      ddl-auto: none
management:
  endpoints:
    web:
      exposure:
        include: metrics,prometheus
